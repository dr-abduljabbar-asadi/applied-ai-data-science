================================================================================
MASTER PROMPT — "Linear Regression from Scratch" Teaching Notebook
================================================================================
Paste everything below to an AI coding assistant (with access to your workspace)
to regenerate the clean, final tutorial notebook in one shot.

--------------------------------------------------------------------------------
ROLE
--------------------------------------------------------------------------------
You are an expert Python, machine-learning, data-visualisation and technical-
education assistant. Produce a single, polished Jupyter notebook that teaches
linear regression from scratch. The notebook must run top-to-bottom without
errors and be suitable for teaching beginners and intermediate learners.

--------------------------------------------------------------------------------
SOURCE
--------------------------------------------------------------------------------
Base it on this compact "ML from scratch" script (pure NumPy, gradient descent):

    class LinearRegression:  fit() runs batch gradient descent; predict() = X·w+b
    r2_score(), mean_squared_error()  as standalone functions
    Demo: sklearn make_regression (1 feature) -> train/test split -> fit -> plot

Preserve the model's gradient-descent logic. You MAY improve code quality and
fix weak spots (see "CODE QUALITY").

--------------------------------------------------------------------------------
OUTPUT
--------------------------------------------------------------------------------
Create/overwrite the notebook:  ml_fundementals/liner_regression.ipynb
- Alternate Markdown and Code cells. Every code cell is followed by a short
  plain-English explanation. Add a "Key insight" line after each chart.
- Number the top-level sections sequentially 1 -> 16 (no gaps, no "0").
- Must execute cleanly from top to bottom (Run All).

--------------------------------------------------------------------------------
ENVIRONMENT
--------------------------------------------------------------------------------
- Python 3.14+.
- Packages: numpy, pandas, matplotlib, seaborn, scikit-learn, plotly, statsmodels
  (statsmodels is required for the Plotly OLS trendline).
- If a package is missing, install it into the notebook's active kernel.

--------------------------------------------------------------------------------
NOTEBOOK STRUCTURE (exact section order)
--------------------------------------------------------------------------------
TITLE CELL
  - Title, one-line subtitle, Overview (what it teaches / who for / task type =
    Regression), Learning outcomes, "What you will build", and a
    "How to use this notebook" call-out (run top-to-bottom; explanations follow
    every code cell; animations have a play button; watch for Key insights).

1. THE CONCEPT IN PLAIN LANGUAGE
   - y_hat = w*x + b with w (slope) and b (intercept) explained.
   - An "Everyday examples" table across >= 6 domains (real estate, retail,
     education, health, energy, transport, agriculture: input x -> predicted y).
   - Two analogies (balancing a ruler over pins; a line of best guess).
   - Short "how it learns: gradient descent" + key terms glossary.

1b. INTUITION ANIMATION (self-contained; imports NumPy/Matplotlib itself so it
    runs even before the Setup section):
   - matplotlib.animation sweeping a line's SLOPE through ~15 fixed points,
     drawing red residual bars and showing the total squared error in the title.
   - Render inline with HTML(anim.to_jshtml()). Add a "what to notice" note.

2. THE MACHINE-LEARNING WORKFLOW
   - A mermaid flowchart (load -> explore -> split -> train -> predict ->
     evaluate -> visualise -> interpret).
   - A table mapping each step to its section number (keep numbers consistent!).
   - Note on feature scaling for multi-feature data.

3. SETUP — imports (numpy, pandas, matplotlib, seaborn; sklearn split/scaler/
   datasets). Set a seaborn theme and %matplotlib inline.

4. EVALUATION METRICS (define first, reuse later)
   - r2_score using the STANDARD definition R2 = 1 - SS_res/SS_tot (note this
     is better than the original corrcoef**2 because it can go negative).
   - mean_squared_error. Both: type hints, NumPy-style docstrings,
     np.asarray(..., dtype=float).

5. THE MODEL — LinearRegression from scratch
   - Full type hints + NumPy-style docstrings on the class and every method.
   - Use np.asarray(..., dtype=float) and the @ matrix-multiply operator.
   - Record self.loss_history_ (MSE each iteration; trailing-underscore = sklearn
     "set during fit" convention).
   - fit() returns self (chainable).
   - predict() raises RuntimeError if called before fit().

6. THE DATA + TRAIN/TEST SPLIT
   - sklearn make_regression(n_samples=100, n_features=1, noise=20,
     random_state=42); train_test_split(test_size=0.2, random_state=1234).

7. EXPLORATORY DATA ANALYSIS (synthetic)
   - DataFrame; print shape / missing / duplicates; head(); describe().
   - Seaborn 1x2: target histogram (KDE) + feature-vs-target scatter.

8. TRAIN THE MODEL — fit on train (1000 iters, lr=0.01); predict on test.

9. EVALUATE — print MSE and R2; explain how to read them.
   - LEARNING CURVE (matplotlib): plot reg_model.loss_history_ (loss per
     iteration); print first/last loss; explain the steep-drop-then-flatten shape.

10. VISUALISING THE FIT
   - Matplotlib: scatter train + test, overlay the learned line, labels/legend.
   - ACTUAL vs PREDICTED (matplotlib): scatter with a 45-degree "perfect
     prediction" dashed line, equal aspect.
   - RESIDUAL ANALYSIS (Yellowbrick style, matplotlib): main panel = residuals
     vs predicted for BOTH train (blue) and test (green) with each set's R2 in
     the legend, black zero line; a RIGHT marginal HISTOGRAM of residuals that
     shares the y-axis. Print mean/std of test residuals. Key insight covers
     patternless residuals, bell-shaped histogram centred on 0, and train/test
     overlap => generalisation (no overfitting).

11. ANIMATIONS — gradient descent (two complementary views, matplotlib +
    HTML(to_jshtml())):
   (a) DATA SPACE: re-run GD, record the (w,b) line each step, animate the line
       rotating/sliding onto the training points.
   (b) PARAMETER SPACE: the loss curve MSE(w) as a bowl; a red ball rolls DOWN it
       toward the best slope (green dashed line), an orange trail marks steps.
       Use SMALL steps (each step closes ~12% of the gap, ~40 frames) so the
       improvement is gradual and visible. Add "what to notice" notes tying it
       to `w -= learning_rate * gradient` and the learning-rate trade-off.

12. BONUS — REAL DATASET (diabetes)
   - Load sklearn.datasets.load_diabetes (442 patients, 10 features, target =
     disease progression). DataFrame; shape/missing/duplicates; head/describe.
   - EDA (INTERACTIVE PLOTLY):
       * CIRCLE CORRPLOT (R-corrplot style): lower-triangle of the correlation
         matrix as circles; SIZE = |correlation|, COLOUR = sign on a diverging
         red->white->blue scale; hover shows exact values; reversed y-axis;
         equal aspect.
       * BMI vs target scatter (plotly.express) with marginal_x/marginal_y
         histograms AND an OLS trendline (trendline="ols"); print the fitted
         line + R2 via px.get_trendline_results.
       * Print features ranked by correlation with the target.
   - PREPROCESSING: split FIRST, then StandardScaler.fit_transform on train and
     .transform on test (explain no data leakage).
   - TRAIN the SAME model on scaled data (2000 iters, lr=0.1); print MSE + R2.
   - ACTUAL vs PREDICTED (INTERACTIVE PLOTLY): teal points, gray dashed 45-deg
     line, an on-chart annotation box with R2 and MSE, equal aspect.
   - Predict 5 test patients (print predicted vs actual) and build a weights
     Series.
   - FEATURE IMPORTANCE (INTERACTIVE PLOTLY): horizontal bars sorted by
     magnitude (most influential on top, via categoryarray), value labels
     outside each bar, teal = pushes up (+) / coral = pulls down (-), a colour
     legend (two dummy traces), a zero reference line, two-line title.

13. KEY TAKEAWAYS (bullets, incl. "real data is messier: lower R2, scale
    features, read weights").
14. COMMON MISTAKES & TROUBLESHOOTING (markdown table: nan/exploding loss,
    under-learning, unscaled features, overfitting, curved residuals, scaler
    leakage).
15. EXERCISES (>= 7, incl. learning-rate sweep, noise levels, loss curve, more
    iterations, compare to sklearn LinearRegression, single-feature diabetes,
    feature-importance vs correlation).
16. NEXT STEPS (compare to sklearn, scaling everywhere, learning curves,
    Ridge/Lasso, tune lr/iterations) + a closing provenance note.

--------------------------------------------------------------------------------
VISUALISATION RULES (apply throughout)
--------------------------------------------------------------------------------
- Choose the best library per chart; don't default to one:
    * Plotly for the INTERACTIVE EDA/results in the diabetes section (corrplot,
      BMI scatter with marginals + OLS, actual-vs-predicted, feature importance).
    * Matplotlib for the custom fit plot, residual analysis, and all animations.
    * Seaborn for quick statistical distributions/scatters in the synthetic EDA.
- Every chart: clear title, axis labels, readable colours, a short interpretation
  and a "Key insight" line afterwards.
- CENTER all Plotly titles: title=dict(x=0.5, xanchor="center")  (the
  plotly_white template left-aligns by default).
- Bar charts: sort by magnitude, add value labels, de-clutter spines, use a
  light grid behind the bars, and a legend explaining colours.
- Actual-vs-predicted charts: equal aspect + matched ranges so the 45-deg line
  is a fair reference; annotate R2/MSE on the chart.
- Animations: render inline via HTML(anim.to_jshtml()) (no ffmpeg needed) and
  call plt.close(fig) before displaying to avoid a duplicate static figure.

--------------------------------------------------------------------------------
CODE QUALITY (Python best practices)
--------------------------------------------------------------------------------
- Type hints + NumPy-style docstrings on all functions/classes.
- Modern NumPy: np.asarray(..., dtype=float), the @ operator.
- Validate at boundaries (predict before fit -> clear error). Don't over-engineer.
- Keep code beginner-friendly and split into logical, top-to-bottom cells.
- No unused imports; return self from fit for chaining.

--------------------------------------------------------------------------------
ACCEPTANCE CRITERIA
--------------------------------------------------------------------------------
1. Runs top-to-bottom with no errors on Python 3.14+.
2. Section headings are sequential 1..16; the workflow table's section numbers
   match the actual sections.
3. Three animations play inline; four Plotly charts render with CENTERED titles.
4. R2 uses the standard 1 - SS_res/SS_tot definition; the model records
   loss_history_, returns self, and validates predict-before-fit.
5. Diabetes example reports a realistic R2 (~0.45) and a feature-importance chart
   where bmi/s5 are the strongest positive drivers.

(Optional) After building, commit the notebook to git and push.
================================================================================
